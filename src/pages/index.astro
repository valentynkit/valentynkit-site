---
import { getCollection } from 'astro:content';
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import FormattedDate from '../components/FormattedDate.astro';
import Header from '../components/Header.astro';
import Subscribe from '../components/Subscribe.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '../consts';

const posts = (await getCollection('blog')).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
);
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<Header />
		<main id="main">
			<section class="intro rise">
				<p class="eyebrow">systems engineer · c · rust · solana</p>
				<h1 class="sr-only">Valentyn Kit</h1>
				<p class="lede">
					I'm <strong>Valentyn Kit</strong>, a distributed-systems and platform
					engineer. I write about systems from the metal up: C and Rust,
					operating systems, networking, databases, and building things from
					scratch to understand them. One level below and one level above.
				</p>
				<p class="meta">
					<a href="https://github.com/valentynkit">github</a> ·
					<a href="https://www.linkedin.com/in/valentynkit">linkedin</a> ·
					<a href="/resume">résumé</a>
				</p>
			</section>

			<p class="eyebrow writing-label">writing</p>
			<ul class="entries">
				{
					posts.map((post, i) => (
						<li class="rise" style={`animation-delay:${Math.min(120 + i * 60, 480)}ms`}>
							<span class="when">
								<FormattedDate date={post.data.pubDate} />
							</span>
							<div class="what">
								<a class="headline" href={`/blog/${post.id}/`}>
									{post.data.title}
								</a>
								<p class="summary">{post.data.description}</p>
							</div>
						</li>
					))
				}
			</ul>
			<Subscribe />
		</main>
		<Footer />
	</body>
</html>
<style>
	.lede {
		font-size: 1.1rem;
		color: var(--fg);
		margin: 0 0 1rem;
	}
	.writing-label {
		margin-top: 3.5rem;
	}
</style>
